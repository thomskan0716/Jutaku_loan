<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="migration.mapper.target.申込担保情報ＰＤＦTargetMapper">

    <!-- 1:N per (申込番号, 申込目的). 申込番号 2→3 and 申込目的 converted by service. -->
    <insert id="insert" parameterType="migration.domain.target.申込担保情報ＰＤＦTarget">
        INSERT INTO 申込担保情報ＰＤＦ (
            申込番号,
            申込目的,
            ファイル種別,
            ファイル名称,
            データファイル名
        ) VALUES (
            #{申込番号},
            #{申込目的},
            #{ファイル種別},
            #{ファイル名称},
            #{データファイル名}
        )
    </insert>

</mapper>
