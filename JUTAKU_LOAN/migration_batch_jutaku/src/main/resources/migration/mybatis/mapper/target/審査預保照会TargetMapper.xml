<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="migration.mapper.target.審査預保照会TargetMapper">

    <!-- Source 審査預保照会. 1:N per (申込番号, 申込目的). 申込番号 2->3 and 申込目的 converted by service. -->
    <insert id="insert" parameterType="migration.domain.target.審査預保照会Target">
        INSERT INTO 審査預保照会 (
            申込番号, 申込目的, イベント, イベント日時, 照会依頼番号
        ) VALUES (
            #{申込番号}, #{申込目的}, #{イベント}, #{イベント日時}, #{照会依頼番号}
        )
    </insert>

</mapper>
