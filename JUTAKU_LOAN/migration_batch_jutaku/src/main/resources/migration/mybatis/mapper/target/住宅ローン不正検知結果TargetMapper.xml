<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="migration.mapper.target.住宅ローン不正検知結果TargetMapper">

    <!-- 1:N per (申込番号, 申込目的). 申込番号 2->3 and 申込目的 converted by service. -->
    <insert id="insert" parameterType="migration.domain.target.住宅ローン不正検知結果Target">
        INSERT INTO 住宅ローン不正検知結果 (
            申込番号, 申込目的, イベント, イベント日時, リクエストＩＤ, 得点, エラーコード, メッセージ
        ) VALUES (
            #{申込番号}, #{申込目的}, #{イベント}, #{イベント日時}, #{リクエストＩＤ}, #{得点}, #{エラーコード}, #{メッセージ}
        )
    </insert>

</mapper>
