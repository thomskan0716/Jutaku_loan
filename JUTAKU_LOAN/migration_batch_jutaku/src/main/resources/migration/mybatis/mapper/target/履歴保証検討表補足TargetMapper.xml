<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="migration.mapper.target.履歴保証検討表補足TargetMapper">

    <!-- Phase 1: PKs only. Data columns (特殊案件区分, 借入額, etc.) added in Phase 2. -->
    <insert id="insert" parameterType="migration.domain.target.履歴保証検討表補足Target">
        INSERT INTO 履歴保証検討表補足 (
            申込番号,
            申込目的,
            回数
        ) VALUES (
            #{申込番号},
            #{申込目的},
            #{回数}
        )
    </insert>

</mapper>
