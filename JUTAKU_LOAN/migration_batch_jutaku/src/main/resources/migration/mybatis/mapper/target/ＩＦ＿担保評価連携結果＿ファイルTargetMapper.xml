<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="migration.mapper.target.ＩＦ＿担保評価連携結果＿ファイルTargetMapper">

    <!-- Table name uses full-width underscores ＿ (U+FF3F) — double-quoted in Oracle SQL.
         1:N per (申込番号, 申込目的). 申込番号 2→3 and 申込目的 converted by service.
         一連番号 is set to the fixed value '99999' by the service. -->
    <insert id="insert" parameterType="migration.domain.target.ＩＦ＿担保評価連携結果＿ファイルTarget">
        INSERT INTO "ＩＦ＿担保評価連携結果＿ファイル" (
            申込番号,
            申込目的,
            一連番号,
            イベント,
            イベント日時,
            ファイル種別,
            ファイル名称,
            データファイル名
        ) VALUES (
            #{申込番号},
            #{申込目的},
            #{一連番号},
            #{イベント},
            #{イベント日時},
            #{ファイル種別},
            #{ファイル名称},
            #{データファイル名}
        )
    </insert>

</mapper>
