<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="migration.mapper.target.申込審査履歴TargetMapper">

    <!-- 1:N event log per (申込番号, 申込目的). 申込番号 2→3 and 申込目的 converted by service. -->
    <insert id="insert" parameterType="migration.domain.target.申込審査履歴Target">
        INSERT INTO 申込審査履歴 (
            申込番号,
            申込目的,
            イベント,
            イベント日時,
            進捗コード,
            ユーザＩＤ,
            ユーザ名,
            回数
        ) VALUES (
            #{申込番号},
            #{申込目的},
            #{イベント},
            #{イベント日時},
            #{進捗コード},
            #{ユーザＩＤ},
            #{ユーザ名},
            #{回数}
        )
    </insert>

</mapper>
