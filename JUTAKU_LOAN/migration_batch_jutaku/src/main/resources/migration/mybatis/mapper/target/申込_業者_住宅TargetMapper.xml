<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="migration.mapper.target.申込_業者_住宅TargetMapper">

    <!-- Table name uses full-width underscores ＿ (U+FF3F) — double-quoted in Oracle SQL -->
    <insert id="insert" parameterType="migration.domain.target.申込_業者_住宅Target">
        INSERT INTO "申込＿業者＿住宅" (
            申込番号,
            申込目的
        ) VALUES (
            #{申込番号},
            #{申込目的}
        )
    </insert>

</mapper>
